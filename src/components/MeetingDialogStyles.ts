import { makeStyles } from '@mui/styles';

const useStyles = makeStyles({
  dialogContent: {
    display: 'flex',
    flexDirection: 'row',
    alignContent: 'center',
    alignItems: 'center',
    paddingBottom: '5px',
  },
});

export default useStyles;
